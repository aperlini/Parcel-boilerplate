{
    "name": "parcel-boilerplate",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf dist && parcel build public/index.html",
        "start": "rm -rf ./dev && rm -rf ./.cache && parcel public/index.html --out-dir dev"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "parcel-bundler": "^1.3.1",
        "postcss-modules": "^3.2.2",
        "postcss-preset-env": "^7.4.2"
    }
}